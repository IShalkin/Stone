version: '3.8'

services:
  storywalker:
    build: .
    ports:
      - "3000:3000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - VITE_GOOGLE_MAPS_KEY=${VITE_GOOGLE_MAPS_KEY:-}
    env_file:
      - .env
    restart: unless-stopped
